# Optional. For local dev or if Railway is behind a proxy.
# BASE_URL=https://ddl-production-47d3.up.railway.app

# Optional. How long to keep received files (ms). Default 3600000 (1 hour).
# FILE_TTL_MS=3600000

# No Zoho auth needed — Zoho POSTs the file to us using its own connection.
